<!-- Copyright 2019 Ecosoft
    License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="action_invoice_all_tree" model="ir.actions.act_window">
        <field name="name">Invoices for Netting</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field
            name="domain"
        >[('state', '=', 'posted'), ('payment_state', "!=", "paid"), ("move_type", "in", ["in_invoice", "out_invoice"])]</field>
        <field name="context">{'default_move_type': 'out_invoice', 'netting': 1}</field>
        <field name="search_view_id" ref="account.view_account_invoice_filter" />
        <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
            Create a customer invoice
        </p><p>
            Create invoices, register payments and keep track of the discussions with your customers.
        </p>
        </field>
    </record>

    <menuitem
        action="action_invoice_all_tree"
        id="menu_action_invoice_all_tree"
        parent="account.menu_finance_entries"
        sequence="50"
    />

</odoo>
