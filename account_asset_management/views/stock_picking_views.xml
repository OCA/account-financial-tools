<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_asset_form" model="ir.ui.view">
        <field name="name">stock.picking.asset.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_asset_ids"
                    type="object"
                    icon="fa-paperclip"
                    attrs="{'invisible': [('count_assets', '=', 0)]}">
                    <field string="Possible assets" name="count_assets" widget="statinfo"/>
                </button>
                <button
                    name="action_view_asset"
                    type="object"
                    icon="fa-paperclip"
                    attrs="{'invisible': [('count_in_assets', '=', 0)]}">
                    <field string="View assets" name="count_in_assets" widget="statinfo"/>
                </button>
            </xpath>
            <xpath expr="//field[@name='move_line_ids']/tree/field[@name='product_id']" position="after">
                 <field name="asset_id"/>
            </xpath>
            <xpath expr="//field[@name='move_lines']/tree/field[@name='product_id']" position="after">
                 <field name="asset_profile_id"/>
            </xpath>
        </field>
    </record>
</odoo>