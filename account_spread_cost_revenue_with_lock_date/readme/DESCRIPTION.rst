This module extends the functionality of `account_spread_cost_revenue` to prevent the automatic creation of journal entries
by scheduled actions if the entries fall before the account lock date. This ensures compliance with accounting rules
and prevents unintended modifications to locked periods.
